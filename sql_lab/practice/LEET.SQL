SELECT (SELECT PRODUCT_NAME FROM PRODUCTS WHERE PRODUCT_ID = O.PRODUCT_ID) AS "product_name"
FROM ORDERS O
WHERE TRUNC(ORDER_DATE,'MONTH') = TO_DATE('FEB-2020','MON-YYYY')
GROUP BY PRODUCT_ID
HAVING SUM(UNIT) >= 100
;